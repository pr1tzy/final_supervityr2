# Source provenance

This tree was **copied** (not moved) on 2026-05-16 for a clean GitHub publish. Original folders were not modified.

| Path here | Copied from |
|-----------|-------------|
| `app/`, `docs/`, `scripts/`, `tests/`, `packages/`, root backend files | `Hackathon/SupervityR2/` (excluding `frontend/` template) |
| `frontend/` | `Hackathon/front_twist/SupervityR2/frontend/` (Twisty Command Center) |

Excluded from copy: `.git`, `.env`, `.env.local`, `node_modules`, `.next`, `__pycache__`, `venv`, `.cursor`.

After copy, only files under `final_supervityr2/` were edited (`README.md`, `DEMO.md`, `.env.local.example`, `.gitignore`).
